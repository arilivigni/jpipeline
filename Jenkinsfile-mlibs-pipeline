@Library('github.com/arilivigni/jpipeline@master') import org.centos.jpipeline.*
@Library('github.com/CentOS/cico-pipeline-library@master') import org.centos.jpipeline.*


node {
    deleteDir()
    cicoCiPipeline {

    }
}